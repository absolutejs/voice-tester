{
	"name": "@absolutejs/voice-tester",
	"version": "0.0.1-beta.1",
	"description": "AI-driven automated tester for @absolutejs/voice services — simulates a caller end-to-end (Twilio Media Stream protocol or real Twilio outbound), drives the conversation with TTS + STT + LLM. Built for the AbsoluteJS AI Studio.",
	"author": "Alex Kahn",
	"license": "MIT",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"bin": {
		"voice-tester": "./dist/cli.js"
	},
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		},
		"./scenarios": {
			"types": "./dist/scenarios/index.d.ts",
			"import": "./dist/scenarios/index.js"
		}
	},
	"files": [
		"dist",
		"src",
		"README.md"
	],
	"scripts": {
		"build": "rm -rf dist && bun build src/index.ts src/cli.ts src/scenarios/index.ts --outdir dist --target node --format esm --external '@absolutejs/*' && bun x tsc -p tsconfig.json --emitDeclarationOnly --outDir dist",
		"typecheck": "bun x tsc -p tsconfig.json --noEmit",
		"prepublishOnly": "bun run build"
	},
	"keywords": [
		"absolutejs",
		"voice",
		"testing",
		"twilio",
		"deepgram",
		"ai",
		"telephony"
	],
	"peerDependencies": {
		"@absolutejs/ai": "*"
	},
	"devDependencies": {
		"@absolutejs/ai": "*",
		"@types/bun": "latest",
		"typescript": "^5.6.0"
	},
	"engines": {
		"bun": ">=1.1.0"
	}
}
